import React, { PropTypes } from 'react';
import SideNav from './SideNav';

const Home = React.createClass({
    render () {
        return (
            <SideNav />
        );
    }
});

export default Home;
